{% extends 'base.html.twig' %}

{% block body %}
    <h1>Domain</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domain.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ domain.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domain.name }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ domain.start }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ domain.end }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ domain.description }}</td>
            </tr>
            <tr>
                <th>Sequence</th>
                <td>{{ domain.sequence }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_domain_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_domain_edit', { 'id': domain.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
